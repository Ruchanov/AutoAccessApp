import { InputsCheckerDirective } from "./inputs-checker.directive";

describe('InputsCheckerDirective', () => {
  it('should create an instance', () => {
    // const directive = new InputsCheckerDirective();
    // expect(directive).toBeTruthy();
  });
});
